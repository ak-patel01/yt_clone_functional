

export default function Card({  }){        //movie

  return(
    <div className="flex flex-col items-center justify-between 
    hover:scale-110 transition duration-300 ease-in gap-3 p-4 mt-10 ml-4 rounded-xl outline">
      {/* <div>
        <p className="text-green-600 font-semibold text-md text-left truncate w-40 mt-1">{movie.list[0].title}</p>
      </div>
      <div className="h-[280px]">
        <img src={movie.list[0].image} className="h-full w-full " />
      </div>

      <div className="flex justify-between gap-12 items-center w-full mt-5">
        <div>
          <p className="text-black font-semibold">Release: {movie.date}</p>
        </div>
        <button className="text-gray-700 border-2 border-gray-700 rounded-full font-semibold 
          text-[12px] p-1 px-3 uppercase 
          hover:bg-gray-700
          hover:text-white transition duration-300 ease-in"
        ><a href={movie.list[0].link}>View</a></button>
      </div> */}
    </div>
  )
}